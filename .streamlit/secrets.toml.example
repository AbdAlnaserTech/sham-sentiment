# Streamlit Cloud → App → Settings → Secrets
# Paste this block (adjust values as needed):

SENTIMENT_CLOUD = "1"
SENTIMENT_CLOUD_LIGHT = "1"
SENTIMENT_MAX_BATCH = "100"

# Optional API protection (FastAPI only, not Streamlit UI):
# SENTIMENT_API_KEY = "your-secret-key"
