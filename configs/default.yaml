# ═══════════════════════════════════════════════════════════════════════════
# إعدادات المشروع — configs/default.yaml
# ═══════════════════════════════════════════════════════════════════════════

# ── بيانات التدريب والمسارات ───────────────────────────────────────────────
data:
  synthetic_counts:
    en: 1700
    ar_fusha: 1700
    ar_shami: 1600
  default_dataset: data/sentiment_dataset_multilingual.csv
  validation_path: data/real/validation_comments.csv
  sample_path: data/sample_comments.csv
  astd_path: data/real/astd_processed.csv

# ── إعدادات التحليل (BERT) ───────────────────────────────────────────────────
inference:
  confidence_threshold: 55.0
  max_text_length: 5000
  bert_neutral_threshold: 0.58
  use_finetuned: true
  default_model: bert
  max_batch_size: 2000

# ── منصة: تسجيل دخول، تنبيهات، روابط ───────────────────────────────────────
platform:
  require_login: true
  negative_alert_threshold: 0.45
  api_key: ""
  github_url: "https://github.com/AbdAlnaserTech/sham-sentiment"
  app_url: "https://sham-sentiment-nas.streamlit.app"

# ── واجهة المستخدم ───────────────────────────────────────────────────────────
ui:
  default_language: auto
  rtl_languages:
    - ar_fusha
    - ar_shami
  university_name_ar: "جامعة الشام"
  university_name_en: "Sham University"
  department_ar: "قسم الهندسة المعلوماتية"
  logo_path: assets/logo.png

logging:
  level: INFO
